{
  "version": 2,
  "framework": null,
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && CI=false DISABLE_ESLINT_PLUGIN=true npm run build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
